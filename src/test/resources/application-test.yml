spring:
  activemq:
    broker-url: vm://localhost?broker.persistent=false
    user: admin
    password: admin

  jms:
    listener:
      auto-startup: true
      acknowledge-mode: auto

  # Use different database for tests
  data:
    mongodb:
      host: localhost
      port: 27017
      database: trainer_workload_test  # ← Different DB name
      username: admin
      password: password
      authentication-database: admin

logging:
  level:
    com.jsalva.trainerworkload: DEBUG